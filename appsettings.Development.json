{
  "//": "Override LOCAL. Solo se lee cuando la app corre en Development (dotnet run); en el publish no aplica. appsettings.json lleva SIEMPRE la URL de produccion y no hay que editarlo para probar.",
  "ApiSettings": {
    "BaseUrl": "https://localhost:7089"
  }
}
